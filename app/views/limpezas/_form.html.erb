<%= simple_form_for @limpeza, :html => { :class => 'form-horizontal' } do |f| %>
  <%= f.input :data %>
  <%= f.input :valor %>
  <%= f.input :veiculo_id %>

  <%= f.button :submit, :class => 'btn-primary' %>
  <%= link_to t('.cancel', :default => t("helpers.links.cancel")),
                limpezas_path, :class => 'btn btn-default' %>
<% end %>
